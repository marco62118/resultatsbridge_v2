<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
<!--    "@xml/backup_rules"-->
    <application
        android:name=".BridgeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@null"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TournoiBridgeOnline"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".main.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.TournoiBridgeOnline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".organisateur.CreationTournoiActivity" />
        <activity android:name=".organisateur.SaisieDonnesAvantTournoiActivity" />
        <activity android:name=".organisateur.ParticipationOrganisateurActivity" />

        <activity
            android:name=".organisateur.ListeEquipesActivity"
            android:exported="false"
            android:label="Liste des équipes" />

        <activity android:name=".organisateur.OrganisateurActivity" />

        <activity
            android:name=".client.ClientActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />

        <activity android:name=".organisateur.ConstitutionEquipesActivity"
            android:exported="false" />

        <activity android:name=".client.CameraDetectionActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>